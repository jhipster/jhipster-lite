import { Routes } from '@angular/router';

const routes: Routes = [
  {
    path: 'health',
    loadComponent: () => import('./health/health.component'),
    title: 'Health',
  },
];

export default routes;
