export const isWindow = () => typeof window !== 'undefined';

export const isLocalStorageEnabled = () => isWindow() && window.localStorage;

export const getLocalStorage = (key: string) => isLocalStorageEnabled() && window.localStorage.getItem(key);

export const setLocalStorage = (key: string, value: string) => isLocalStorageEnabled() && window.localStorage.setItem(key, value);

export const removeLocalStorage = (key: string) => isLocalStorageEnabled() && window.localStorage.removeItem(key);
